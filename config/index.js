const path = require('path')

const config = {
    dev: {
        env: {
            NODE_ENV: '"development"'
        },
        port: 8000,
        utils_paths:{
            
        }
    },
    prod:{

    }
}

module.exports = config