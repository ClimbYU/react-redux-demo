const path = require('path')

const config = {
    dev: {
        env: {
            NODE_ENV: '"development"'
        },
        port: 8080,
        utils_paths:{
            
        }
    },
    prod:{

    }
}

module.exports = config